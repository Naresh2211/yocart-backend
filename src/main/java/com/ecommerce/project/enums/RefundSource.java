package com.ecommerce.project.enums;

public enum RefundSource {

    // Refund triggered because user cancelled a paid order
	CANCELLATION,

    // Refund triggered because of return request
    RETURN
}
